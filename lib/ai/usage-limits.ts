// Client-safe constants only — no server imports, so this can be used from
// client components (e.g. the pricing table) as well as server code.

/** Free-tier AI tool uses per calendar month (site-build-prompt.md section 6). */
export const FREE_MONTHLY_AI_USES = 5;
